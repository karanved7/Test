[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "cli-toolkit"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["click>=8.0"]

[project.scripts]
toolkit = "cli.main:cli"

[tool.pytest.ini_options]
testpaths = ["tests"]
